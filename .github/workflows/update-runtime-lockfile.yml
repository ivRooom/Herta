name: Update Runtime Lockfile

on:
  push:
    branches: [security/minimize-runtime-image]
    paths: [.github/workflows/update-runtime-lockfile.yml]

permissions:
  contents: write

jobs:
  update-lockfile:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: security/minimize-runtime-image

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --lockfile-only

      - name: lockfileをコミット
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "lockfile差分はありません"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pnpm-lock.yaml
          git commit -m "build(runtime): Prisma CLIのproduction依存をlockfileへ反映"
          git push origin HEAD:security/minimize-runtime-image
