name: Temporary Mini Games v2 atomic metrics

on:
  push:
    branches:
      - feature/mini-games-v2-stats-challenges

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/mini-games-v2-stats-challenges
      - run: python3 .github/scripts/temp-mini-games-v2-atomic-metrics.py
      - name: Remove temporary files and commit
        run: |
          rm .github/scripts/temp-mini-games-v2-atomic-metrics.py
          rm .github/workflows/temporary-mini-games-v2-atomic-metrics.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/bot/src/plugins .github
          git commit -m "Mini Gamesの関連戦績をtransactionで保存する"
          git push origin HEAD:feature/mini-games-v2-stats-challenges
