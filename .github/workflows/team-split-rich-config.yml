name: Team Split Rich Config

on:
  push:
    branches:
      - feat/team-split-rich-config
    paths:
      - scripts/patch-team-split-rich-config.py
      - .github/workflows/team-split-rich-config.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/team-split-rich-config
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:generate
      - run: python3 scripts/patch-team-split-rich-config.py
      - run: pnpm exec prettier --write apps/studio/src/components/team-split-manager.tsx apps/studio/src/app/dashboard/guilds/'[guildId]'/team-split/page.tsx
      - run: pnpm --dir apps/studio typecheck
      - name: Commit and remove temporary files
        shell: bash
        run: |
          set -euo pipefail
          rm scripts/patch-team-split-rich-config.py
          rm .github/workflows/team-split-rich-config.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/studio scripts/patch-team-split-rich-config.py .github/workflows/team-split-rich-config.yml
          git commit -m "feat: Team Split設定を検索式UIへ移行"
          git push origin HEAD:feat/team-split-rich-config
