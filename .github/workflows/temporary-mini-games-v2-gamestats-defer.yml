name: Temporary Mini Games v2 gamestats defer

on:
  push:
    branches:
      - feature/mini-games-v2-stats-challenges

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/mini-games-v2-stats-challenges
      - run: python3 .github/scripts/temp-mini-games-v2-gamestats-defer.py
      - name: Remove temporary files and commit
        run: |
          rm .github/scripts/temp-mini-games-v2-gamestats-defer.py
          rm .github/workflows/temporary-mini-games-v2-gamestats-defer.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/bot/src/plugins .github
          git commit -m "gamestatsをDB集計前にACKする"
          git push origin HEAD:feature/mini-games-v2-stats-challenges
