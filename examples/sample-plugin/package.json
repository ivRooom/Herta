{
  "name": "@herta/example-sample-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "test": "echo 'no tests configured'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@herta/db": "workspace:*",
    "@herta/plugin-sdk": "workspace:*",
    "@herta/shared": "workspace:*",
    "discord.js": "^14.26.5"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
